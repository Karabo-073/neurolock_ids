# neurolock_ids
NeuroLock is a real-time Intrusion Detection System built in C++ using libpcap. It monitors network traffic on the host machine, detecting port scans, ICMP floods, and suspicious application-layer payloads such as login attempts. 
